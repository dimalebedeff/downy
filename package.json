{
  "name": "downy",
  "private": true,
  "version": "0.8.0",
  "description": "Расширение для скачивания видео и аудио с любых сайтов",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "coapp:install": "node coapp/install.mjs",
    "coapp:fetch-bins": "node coapp/fetch-bins.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
